services:
  - type: web
    name: your-app-name
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn fyp.wsgi:application
    workingDir: .
    autoDeploy: true
